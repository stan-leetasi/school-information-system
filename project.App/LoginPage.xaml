<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="project.App.LoginPage">
    
    <StackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Text="[REFRESH BUTTON]" Margin="20" />
            <Label Grid.Column="1" Text="School information system" HorizontalOptions="CenterAndExpand" 
                   VerticalOptions="CenterAndExpand" />
            <Button Grid.Column="2" Text="Login as admin" Clicked="OnAdminLogin" Margin="20" />
        </Grid>
        
        <Picker x:Name="MyPicker" Grid.Row="1" Title="Select student" HorizontalOptions="CenterAndExpand"
                VerticalOptions="CenterAndExpand" FontSize="Large"
                ItemsSource="{Binding Students}"
                SelectedIndexChanged="OnPickerSelectedItemChanged">
        </Picker>
    </StackLayout>
</ContentPage>
