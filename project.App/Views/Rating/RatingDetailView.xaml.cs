using project.App.ViewModels.Rating;

namespace project.App.Views.Rating;

public partial class RatingDetailView
{
    public RatingDetailView(RatingDetailViewModel viewModel)
        : base(viewModel)
    {
        InitializeComponent();
    }
}