using project.App.ViewModels.Rating;

namespace project.App.Views.Rating;

public partial class RatingEditView
{
	public RatingEditView(RatingEditViewModel viewModel) :base(viewModel)
	{
		InitializeComponent();
	}
}